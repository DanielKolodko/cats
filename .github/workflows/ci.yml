name: CD

on:
  push:
    tags:
      - 'v*'    # triggers on tags that begin with "v"
  workflow_dispatch:  # allows manual triggering

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/cat-gifs-app:latest .

      - name: Log in to Docker Hub
        run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Push Docker image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/cat-gifs-app:latest

      - name: Deploy application
        run: |
          # Replace the next line with your actual deployment commands.
          # For example, you might SSH into a server and run docker-compose up -d,
          # or apply Kubernetes manifests with kubectl.
          echo "Deployment step: Add your deploy commands here."
